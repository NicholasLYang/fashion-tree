<p id="notice"><%= notice %></p>
<div class="body">
<div class="header">
  <h1>Clothes</h1>
</div>
<% @products.each do |product| %>
<div class="square">
  <div class="content">
      <div class="hovereffect">
        <%= image_tag("https://" + product.picture, class: "img-responsive") %>
        <div class="overlay">
           <h2><%= product.name %></h2>
           <%= link_to 'More', product, class: 'info' %>
        </div>
      </div>
  </div>
</div>
<% end %>
</div>
